<!DOCTYPE html>
<html lang="en" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | YouCode Job Dating</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: "#1d4fd7",
                        accent: "#f59e0b",
                        success: "#10b981"
                    },
                    fontFamily: { sans: ['Inter', 'sans-serif'] }
                }
            }
        }
    </script>
    <style>
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.fill { font-variation-settings: 'FILL' 1; }
        body { font-family: 'Inter', sans-serif; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
<link rel="stylesheet" href="/index.css">
</head>
<body class="bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-slate-100 antialiased min-h-screen flex">
    
    <!-- Sidebar Container -->
    <div id="sidebar-container" class="shrink-0 w-72 h-screen sticky top-0"></div>

    <!-- Main Content -->
    <main class="flex-1 h-screen overflow-y-auto p-6 lg:p-10">
        <div class="max-w-7xl mx-auto space-y-8">
            <header class="flex flex-wrap items-center justify-between gap-4">
                <div>
                    <h1 class="text-3xl font-extrabold tracking-tight">Dashboard Overview</h1>
                    <p class="text-slate-500 dark:text-slate-400">Welcome back, here's your recruitment summary.</p>
                </div>
                <a href="/admin/announcements/create" class="bg-primary hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-primary/20 flex items-center gap-2 transition-all active:scale-95">
                    <span class="material-symbols-outlined">add_circle</span>
                    Create Job
                </a>
            </header>

            <!-- Stats Grid -->
            <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                <div class="bg-gradient-to-br from-blue-50 via-blue-100 to-indigo-100 dark:from-blue-950 dark:via-slate-800 dark:to-slate-900 p-6 rounded-2xl border-2 border-blue-200 dark:border-blue-600/40 shadow-lg hover:shadow-xl dark:hover:shadow-blue-900/20 hover:border-blue-400 dark:hover:border-blue-500/60 transition-all group">
                    <div class="size-12 bg-primary/15 dark:bg-blue-900/40 text-primary dark:text-blue-300 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 group-hover:bg-primary/25 dark:group-hover:bg-blue-800/60 transition-all">
                        <span class="material-symbols-outlined fill">campaign</span>
                    </div>
                    <p class="text-[10px] font-black uppercase text-blue-700 dark:text-blue-200 tracking-widest">Active Jobs</p>
                    <h3 class="text-2xl font-black text-blue-900 dark:text-blue-100">{{ stats.active_announcements }}</h3>
                </div>
                <div class="bg-gradient-to-br from-emerald-50 via-green-100 to-teal-100 dark:from-emerald-950 dark:via-slate-800 dark:to-slate-900 p-6 rounded-2xl border-2 border-emerald-200 dark:border-emerald-600/40 shadow-lg hover:shadow-xl dark:hover:shadow-emerald-900/20 hover:border-emerald-400 dark:hover:border-emerald-500/60 transition-all group">
                    <div class="size-12 bg-success/15 dark:bg-emerald-900/40 text-success dark:text-emerald-300 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 group-hover:bg-success/25 dark:group-hover:bg-emerald-800/60 transition-all">
                        <span class="material-symbols-outlined fill">group</span>
                    </div>
                    <p class="text-[10px] font-black uppercase text-emerald-700 dark:text-emerald-200 tracking-widest">Total Candidates</p>
                    <h3 class="text-2xl font-black text-emerald-900 dark:text-emerald-100">{{ stats.students }}</h3>
                </div>
                <div class="bg-gradient-to-br from-amber-50 via-orange-100 to-yellow-100 dark:from-amber-950 dark:via-slate-800 dark:to-slate-900 p-6 rounded-2xl border-2 border-amber-200 dark:border-amber-600/40 shadow-lg hover:shadow-xl dark:hover:shadow-amber-900/20 hover:border-amber-400 dark:hover:border-amber-500/60 transition-all group">
                    <div class="size-12 bg-accent/15 dark:bg-amber-900/40 text-accent dark:text-amber-300 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 group-hover:bg-accent/25 dark:group-hover:bg-amber-800/60 transition-all">
                        <span class="material-symbols-outlined fill">corporate_fare</span>
                    </div>
                    <p class="text-[10px] font-black uppercase text-amber-700 dark:text-amber-200 tracking-widest">Partners</p>
                    <h3 class="text-2xl font-black text-amber-900 dark:text-amber-100">{{ stats.companies }}</h3>
                </div>
            </section>

            <!-- Recent Activity -->
            <section class="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 overflow-hidden">
                <div class="p-6 border-b border-slate-100 dark:border-slate-800 flex items-center justify-between">
                    <h2 class="font-bold">Recent Job Postings</h2>
                    <a href="/admin/announcements" class="text-primary text-xs font-bold hover:underline">View All</a>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead class="bg-gradient-to-r from-blue-100 to-indigo-100 dark:from-slate-700 dark:to-slate-800 text-[10px] font-black uppercase text-blue-900 dark:text-blue-300 border-b-2 border-blue-200 dark:border-blue-500/30">
                            <tr>
                                <th class="px-6 py-4">Title</th>
                                <th class="px-6 py-4">Company</th>
                                <th class="px-6 py-4">Apps</th>
                                <th class="px-6 py-4">Status</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-100 dark:divide-slate-800">{% for a in latest_announcements %}
<tr>
    <td class="px-6 py-4 font-bold text-sm">{{ a.title }}</td>
    <td class="px-6 py-4 text-sm">{{ a.company_name }}</td>
    <td class="px-6 py-4 text-sm">—</td>
    <td class="px-6 py-4">
        <span class="px-2 py-1 bg-success/10 text-success text-[10px] font-black uppercase rounded-lg">
            Active
        </span>
    </td>
</tr>
{% else %}
<tr>
    <td colspan="4" class="px-6 py-4 text-center">No recent announcements</td>
</tr>
{% endfor %}


                        </tbody>
                    </table>
                </div>
            </section>
        </div>
    </main>

    <script src="/assets/js/script.js"></script>
</body>
</html>
