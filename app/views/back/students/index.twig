<!DOCTYPE html>
<html lang="en" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidates | YouCode Job Dating</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
    <script>
        tailwind.config = { darkMode: 'class', theme: { extend: { colors: { primary: "#1d4fd7", accent: "#f59e0b", success: "#10b981" }, fontFamily: { sans: ['Inter', 'sans-serif'] } } } }
    </script>
    <style>
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.fill { font-variation-settings: 'FILL' 1; }
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-slate-100 antialiased min-h-screen flex">
    <div id="sidebar-container" class="shrink-0 w-72 h-screen sticky top-0"></div>
    <main class="flex-1 h-screen overflow-y-auto p-6 lg:p-10">
        <div class="max-w-7xl mx-auto space-y-8">
            <h1 class="text-3xl font-extrabold tracking-tight">Candidates Directory</h1>
            <div class="bg-white dark:bg-slate-800 rounded-2xl border-2 border-slate-200 dark:border-slate-600 overflow-hidden shadow-lg dark:shadow-lg">
                <table class="w-full text-left">
                    <thead class="bg-gradient-to-r from-blue-100 to-indigo-100 dark:from-slate-700 dark:to-slate-800 text-[10px] font-black uppercase text-blue-900 dark:text-blue-300 border-b-2 border-blue-200 dark:border-blue-500/30">
                        <tr>
                            <th class="px-6 py-4">Name</th>
                            <th class="px-6 py-4">Cohort</th>
                            <th class="px-6 py-4">Status</th>
                            <th class="px-6 py-4 text-right">Action</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
  {% for s in students %}
    <tr class="hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors">
      <td class="px-6 py-4">
        <div class="flex items-center gap-3">
          <div class="size-8 rounded-full bg-primary/10 text-primary flex items-center justify-center font-bold text-xs">
            {{ s.name|slice(0,1) }}
          </div>
          <span class="text-sm font-bold">{{ s.name }}</span>
        </div>
      </td>

      <td class="px-6 py-4 text-sm">
        {{ s.promotion }} {{ s.specialization }}
      </td>

      <td class="px-6 py-4">
        <span class="px-2 py-1 bg-success/10 text-success text-[10px] font-black uppercase rounded-lg tracking-wider">
          Active
        </span>
      </td>

      <td class="px-6 py-4 text-right">
        <button class="p-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg">
          <span class="material-symbols-outlined text-sm">more_horiz</span>
        </button>
      </td>
    </tr>
  {% else %}
    <tr>
      <td colspan="4" class="px-6 py-6 text-center text-sm text-slate-500">
        No students found
      </td>
    </tr>
  {% endfor %}
</tbody>

                </table>
            </div>
        </div>
    </main>
    <script src="js/main.js"></script>
  <script src="/assets/js/script.js"></script>
</body>
</html>
