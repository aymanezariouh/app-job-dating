<!DOCTYPE html>
<html lang="en" class="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Archived Jobs | YouCode Job Dating</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
    <script>
        tailwind.config = { darkMode: 'class', theme: { extend: { colors: { primary: "#1d4fd7", accent: "#f59e0b", success: "#10b981" }, fontFamily: { sans: ['Inter', 'sans-serif'] } } } }
    </script>
    <style>
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.fill { font-variation-settings: 'FILL' 1; }
        body { font-family: 'Inter', sans-serif; }
    </style>
    <link rel="stylesheet" href="/index.css">
</head>
<body class="bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-slate-100 antialiased min-h-screen flex">
    <div id="sidebar-container" class="shrink-0 w-72 h-screen sticky top-0"></div>

    <main class="flex-1 h-screen overflow-y-auto p-6 lg:p-10">
        <div class="max-w-7xl mx-auto space-y-8">
            <header class="flex flex-wrap items-center justify-between gap-4">
                <div>
                    <h1 class="text-3xl font-extrabold tracking-tight">Archived Jobs</h1>
                    <p class="text-slate-500 dark:text-slate-400">View and restore archived job listings.</p>
                </div>
                <a href="/admin/announcements" class="bg-slate-600 hover:bg-slate-700 text-white px-6 py-3 rounded-xl font-bold shadow-lg flex items-center gap-2 transition-all active:scale-95">
                    <span class="material-symbols-outlined">arrow_back</span>
                    Back to Active
                </a>
            </header>

            {% if flash_success %}
            <div class="bg-success/10 border-2 border-success/30 text-success px-6 py-4 rounded-xl font-bold">
                {{ flash_success }}
            </div>
            {% endif %}

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                {% for a in announcements %}
                <div class="bg-gradient-to-br from-slate-100 via-slate-100 to-slate-200 dark:from-slate-800 dark:via-slate-800 dark:to-slate-900 p-6 rounded-2xl border-2 border-slate-300 dark:border-slate-600 shadow-lg hover:shadow-xl transition-all flex flex-col justify-between opacity-75">
                    <div>
                        <div class="flex items-center justify-between mb-4">
                            <span class="px-3 py-1.5 bg-slate-200 dark:bg-slate-700 text-slate-600 dark:text-slate-400 text-[10px] font-black uppercase rounded-lg">
                                {{ a.contract_type }}
                            </span>
                            <span class="px-2 py-1 bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-400 text-[10px] font-black uppercase rounded-lg flex items-center gap-1">
                                <span class="material-symbols-outlined text-xs">archive</span>
                                Archived
                            </span>
                        </div>

                        <h3 class="text-lg font-bold text-slate-700 dark:text-slate-300">{{ a.title }}</h3>
                        <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">
                            {{ a.company_name }} • {{ a.location }}
                        </p>
                    </div>

                    <div class="mt-6 flex items-center justify-end pt-4 border-t-2 border-slate-300 dark:border-slate-700">
                        <form method="post" action="/admin/announcements/restore" class="inline">
                            <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                            <input type="hidden" name="id" value="{{ a.id }}">
                            <button type="submit" class="text-success text-xs font-bold flex items-center gap-1 hover:underline" onclick="return confirm('Restore this announcement?')">
                                Restore <span class="material-symbols-outlined text-sm">restore</span>
                            </button>
                        </form>
                    </div>
                </div>
                {% else %}
                <div class="col-span-2 text-center py-12 text-slate-500">
                    <span class="material-symbols-outlined text-6xl mb-4 opacity-30">archive</span>
                    <p class="font-bold">No archived jobs.</p>
                </div>
                {% endfor %}
            </div>
        </div>
    </main>

    <script src="/assets/js/script.js"></script>
</body>
</html>
